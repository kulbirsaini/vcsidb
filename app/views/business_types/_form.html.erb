<%= form_for(@business_type) do |f| %>
  <% if @business_type.errors.any? %>
    <div class="row">
      <div class="span14 offset1">
        <div id="error_explanation" class="alert-message block-message error">
          <h3><%= pluralize(@business_type.errors.count, "error") %> prohibited this business_type from being saved:</h3>

          <ul>
          <% @business_type.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
          </ul>
        </div>
      </div>
    </div>
  <% end %>

  <div class="clearfix">
    <%= f.label :name %>
    <div class="input"><%= f.text_field :name %></div>
  </div>
  <div class="clearfix">
    <%= f.label :description %>
    <div class="input"><%= f.text_area :description %></div>
  </div>
  <div class="clearfix">
    <%= f.label :disabled %>
    <div class="input"><%= f.check_box :disabled %></div>
  </div>
  <div class="actions">
    <%= f.submit :class => 'btn primary' %>
  </div>
<% end %>
